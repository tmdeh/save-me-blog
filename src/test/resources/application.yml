spring:
  jpa:
    show-sql: true
    properties:
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://121.151.206.7:3306/blog?useSSL=false&useUnicode=true&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: tmdeh
    password: tmdeh3216
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: savemeyuseungdospringbootblogservice

file:
  path: posts

logging:
  level:
    com:
      zaxxer:
        hikari: debug